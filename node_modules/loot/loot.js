var EventEmitter = require('events').EventEmitter
var _captain = new EventEmitter()
var _looting = 0
/*
	DB fetch helper
	loot.plan(db, id) // plan a db looting
	loot.grab(cb)     // grab all the loot
	_.loot()          // actually grab the loot. private
	_captain          // keep track of looting
*/

this.plan = function plan(db, id){
	// ++looting
	// grab data
	// call loot
}
this.grab = function grab(cb){
	// looting = 0 ? 
	// yes : run cb
	// no  : setup listener on loot:done
}
function loot(){
	// --looting
	// looting==0 ? emit("loot:done")
}

