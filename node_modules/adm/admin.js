var wget = require("request")
var swig = require("swig")
var db   = require("db")
var logly= require("logly")
var p    = logly.log

logly.name("boulangerie")
logly.mode("debug")

this.test = function(req, res){
//	var tpl_val = util.mk_tpl_val(req)
	var tpl = swig.compileFile('/mnt/site/index.html')
	p(JSON.stringify(tpl.render()))
}

this.html = function(req, res){
	var tpl_val = {}
	res.render("admin", tpl_val)
}

