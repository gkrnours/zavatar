{
	"_id": "_design/menu",
	"language": "javascript",

	"validate_doc_update": "function(doc, old, usr){ if(doc.type != \"menu\") return; }",
	"views":{
		"list":{
			"map": "function(doc){ if(doc.type!=\"menu\") return; emit(doc) }"
		}
	}
}
